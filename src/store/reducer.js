import * as actionTypes from "./action";
const initialState = {
  auth: true,
};

const reducer = (state = initialState, action) => {
  switch (actionTypes) {
    default:
      return state;
  }
};
export default reducer;
